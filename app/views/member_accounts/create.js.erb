<% unless @user.valid? %>
    $("#modal_sign_in_or_sign_up_container").html("<%= escape_javascript(render :partial => "sign_in/purchase_manager_form", :locals => {:user => @user, :url => member_accounts_path(:format => :js), :modal_box => true}) %>");
    select_subscription_radio_button('user_member', <%= @user.subscription_plan_id -%>);
<% else %>
    alert("ok")
<% end %>