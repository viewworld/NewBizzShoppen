<div class="frm_tiny pdd_10">

  <p>
    <strong>Value created today: </strong><%= as_currency(AgentInformation.today(current_user).value_created, Currency.euro, 0) %> (<%= as_currency(TopAgent.value_for(:today), Currency.euro, 0) %>),
    this week: <%= as_currency(AgentInformation.week(current_user).value_created, Currency.euro, 0) %> (<%= as_currency(TopAgent.value_for(:week), Currency.euro, 0) %>),
    this quarter: <%= as_currency(AgentInformation.quarter(current_user).value_created, Currency.euro, 0) %> (<%= as_currency(TopAgent.value_for(:quarter), Currency.euro, 0) %>),
    this year: <%= as_currency(AgentInformation.year(current_user).value_created, Currency.euro, 0) %> (<%= as_currency(TopAgent.value_for(:year), Currency.euro, 0) %>)
  </p>

</div>