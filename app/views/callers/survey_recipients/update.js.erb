<% if @survey_recipient.update_attributes(params[:survey_recipient]) %>
    <% @survey_recipient.completed! %>
    window.close();
<% else %>
    $("#survey_recipient_form").html("<%= escape_javascript(render(:partial => "survey_recipient_form", :locals => { :survey_recipient => @survey_recipient })) %>");
<% end %>