<%= content_for :column_mc do -%>
  <%= header(:back => true) do |section| %>
    <% section.block_header do %>
      <%= t("materials.views.index.header", :campaign => @campaign.name) %>
    <% end %>
  <% end %>

  <%= render :partial => "shared/materials/form", :locals => { :material => @material, :materials => @materials, :is_default_enabled => true,
                                                               :upload_url => :callers_campaign_materials_path, :delete_url => :callers_campaign_material_path,
                                                               :campaign => @campaign} %>
<% end %>
