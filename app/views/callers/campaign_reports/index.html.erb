<head:>
  <style type="text/css">
      .c
      {
          width: 1300px;
      }
  </style>
</head:>

<%= render :partial => 'search' %>

<column-mc:>
  <header>
    <param:content>
      <%= t("campaign_reports.index.your_queued_tasks") %>
    </param:content>

    <param:actions>
      <ul class="header_actions">
        <li class="last">
          <%= bt_link_to nil, t("users.delayed_jobs.view_all"), users_delayed_jobs_path %>
        </li>
      </ul>
    </param:actions>
  </header>

  <%= render :partial => 'shared/delayed_jobs/user', :locals => {:user => current_user, :delayed_jobs => current_user.delayed_jobs.limit(5)} %>
</column-mc:>

<%= render :partial => 'js' %>