<ul class="nav">
  <%= main_menu_link_to_role_specific_home_page %>
  <% if params[:slug] or current_user_has_role?(:category_buyer)%>
      <%= main_menu_link_to t("layout.main_menu.shared.browse_leads"), "/#{@home_category.cached_slug}/leads", :tab => "browse_leads" %>
  <% else %>
      <%= main_menu_link_to t("layout.main_menu.shared.browse_leads"), categories_path, :tab => "browse_leads" %>
  <% end %>
  <% if current_user %>
      <% current_user.roles.each do |role| %>
          <%= render "/layouts/menu_sections/#{role.to_s}" %>
      <% end %>
  <% end %>
</ul>