= content_for :column_mc do
  = header(back: true) do |section|
    - section.block_header do
      = t("administration.softphone_servers.new.view.header")

  .box
    .box-header
      h2= t("administration.softphone_servers.new.view.header")

    = render 'form'
