<div class="pdd_10 no_pdd_t">
  <fieldset class="inputs labels_100 labels_ta_r">
    <ol>
      <%= form.input :title %>
      <%= form.input :content,
                        :as => :ckeditor,
                        :label => false,
                        :input_html => {
                            :width => '1085px',
                            :height => '300px',
                            :swf_params => {
                                :resource_type => @article.class.name,
                                :resource_id => @article.id
                                }
                            } %>
      <%= form.input :published %>
    </ol>
  </fieldset>
</div>
