<column-mc:>
  <header><%= t('administration.articles.new.view.header') %></header>

  <%= semantic_form_for :article, @article, :url => administration_articles_path do |form| %>
      <frm title="TODO - header" c="todo_delete">
        <param:content>
          <%= render :partial => 'form', :locals => {:form => form} %>

          <hl/>

        </param:content>
        <param:actions>
          <ibt c="fltri" icon="save">
            <%= form.submit t("administration.articles.new.view.button_create") %>
          </ibt>
        </param:actions>
      </frm>
  <% end %>
</column-mc:>