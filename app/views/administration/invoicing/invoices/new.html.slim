= content_for :column_mc do
  = header(back: true) do |section|
    - section.block_header do
      = t("administration.invoices.new.view.header")
  .clb

  = semantic_form_for [:administration, :invoicing, @invoice] do |form|
    = frm do |section|
      - section.block_content do
        .pdd_10.no_pdd_t
          fieldset.inputs.labels_ta_r.labels_220
            ol.align_inputs
              li.header
                h3= t("administration.invoices.new.view.basic_information")
              = form.inputs do
                = form.input :user_id, collection: User.all, as: :select, include_blank: false
                = form.input :seller_id, collection: Seller.all, as: :select, include_blank: false
                = form.input :currency_id, as: :hidden

      - section.block_actions do
        = ibt(class: "fltri", icon: "save") do
          = form.submit t("administration.invoices.new.view.button_create")
        = ibt(class: "fltri bt_grey") do
          = button_to_function t("common.cancel_link"), "window.location='#{administration_invoicing_upcoming_invoices_path}'"
