<column-mc:>
  <header>New account</header>

  <%= semantic_form_for @user, :url => administration_users_path(:role => params[:role]) do |form| %>
      <frm title="TODO - header" c="todo_delete">
        <param:content>
          <%= render :partial => 'form', :locals => {:form => form} %>

          <hl/>

          <div class="pdd_10 no_pdd_t">
            <fieldset class="inputs labels_ta_r labels_240">
              <ol class="align_inputs">
                <%= form.inputs :password, :password_confirmation %>
              </ol>
            </fieldset>
          </div>

        </param:content>
        <param:actions>
          <ibt c="fltri" icon="save">
            <%= form.submit t("administration.users.edit.view.button_update_user") %>
          </ibt>
        </param:actions>
      </frm>
  <% end %>
</column-mc:>