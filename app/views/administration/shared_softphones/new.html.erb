<%= content_for :column_mc do %>
  <header back="true"><%= t("administration.shared_softphones.new.view.header") %></header>

  <%= semantic_form_for @shared_softphone, :url => administration_shared_softphones_path do |form| %>
      <frm title="">
        <param:content>
          <%= render :partial => 'form', :locals => {:form => form} %>
        </param:content>
        <param:actions>
          <ibt c="fltri" icon="save">
            <%= form.submit t("views.button_create") %>
          </ibt>
          <ibt c="bt_grey fltri">
            <%= button_to_function t("administration.currencies.new.view.button_cancel"), href(administration_shared_softphones_path) %>
          </ibt>
        </param:actions>
      </frm>
  <% end %>
<% end %>
