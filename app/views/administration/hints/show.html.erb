<column-mc:>
  <header back="true"><%= t('administration.hints.show.view.header') %></header>

  <frm title="#{@hint.title}">
    <param:header>
      <%= bt_link_to nil, t("common.edit_link"), edit_administration_article_path, :class => "fltr" %>
    </param:header>
    <param:content>
      <div class="pdd_10">
        <%= raw @hint.content %>
      </div>
    </param:content>
  </frm>

</column-mc:>