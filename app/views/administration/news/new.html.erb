<column-mc:>
  <header><%= t('administration.news.new.view.header') %></header>

  <%= semantic_form_for @news, :url => administration_news_index_path do |form| %>
      <frm title="TODO - header" c="todo_delete">
        <param:content>
          <%= render :partial => 'form', :locals => {:form => form} %>

          <hl/>

        </param:content>
        <param:actions>
          <ibt c="fltri" icon="save">
            <%= form.submit t("administration.news.new.view.button_create") %>
          </ibt>
        </param:actions>
      </frm>
  <% end %>
</column-mc:>