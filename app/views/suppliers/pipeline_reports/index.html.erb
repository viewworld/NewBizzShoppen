<column-mc:>
  <header><%= t('supplier.pipeline_reports.index.view.header') %></header>
  <%= render :partial => 'pipeline_report', :locals => {:result => @pipeline_report.result,
                                                        :currency => @pipeline_report.currency} %>
</column-mc:>
