<column-mc:>

  <%= render :partial => "campaigns/show", :locals => {:path => call_centres_campaign_path(@campaign), :campaign => @campaign,
                                                       :path_for_js => result_details_call_centres_campaign_path(@campaign),
                                                       :search => @search, :date_from => @date_from, :date_to => @date_to,
                                                       :agent_ids => @agent_ids, :final => @final,
                                                       :headers => @headers, :results => @results,
                                                       :path_back => call_centres_campaigns_path } %>

</column-mc:>