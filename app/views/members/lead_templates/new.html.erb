<%= content_for :column_mc do -%>
  <header back="true"><%= t("member.lead_templates.new.view.header") %><%= image_tag_for_locale %></header>
  <%= semantic_form_for [:members, @lead_template], :method => :put do |form| %>
      <frm title="">
        <param:content>
          <%= render :partial => '/shared/lead_templates/form', :locals => {:form => form} %>
        </param:content>
        <param:actions>
          <ibt c="fltri" icon="plus">
            <%= form.submit t("member.lead_templates.new.view.button_create") %>
          </ibt>
          <ibt c="bt_grey fltri">
            <%= button_to_function t("member.lead_templates.new.view.button_cancel"), "history.go(-1)" %>
          </ibt>
        </param:actions>
      </frm>
  <% end %>
<% end %>