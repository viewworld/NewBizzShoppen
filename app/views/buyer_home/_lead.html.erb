<li>
  <h4><%= link_to lead.header, lead_path(lead) %></h4>
  <p class="element">
    <%= t("buyer_home.show.view.purchase_value_label") %>
    <span class="green"><%= lead.purchase_value %></span>
  </p>
  <p class="element">
    <%= t("buyer_home.show.view.lead_price_label") %>
    <span class="green"><%= lead.price %></span>
  </p>
  <%#= t("buyer_home.show.view.description_label") %> <%#= lead.description %>
  <%= link_to t("buyer_home.show.view.read_more_link"), lead_path(lead) %>
</li>
