<column-mc:>
  <header><%= t("comments.leads.show.view.header", :header => @lead.header) %></header>
  <% @threads.each do |thread| %>
    <%= render :partial => 'comments/shared/thread', :locals => {:thread => thread} %>
  <% end %>
</column-mc:>