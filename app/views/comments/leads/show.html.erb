<column-mc:>
  <header back="true"><%= t("comments.leads.show.view.header", :header => @lead.header) %></header>
  <% @threads.each do |thread| %>
    <%= render :partial => 'comments/shared/thread', :locals => {:thread => thread} %>
      <hr/>
  <% end %>

  <%= render :partial => "comments/threads/new" %>
</column-mc:>