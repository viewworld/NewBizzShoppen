<% SubscriptionPlan.active.for_role(role).each do |subscription_plan| %>
    <div id="subscription_plan_<%= subscription_plan.id %>">
    <h3><%= subscription_plan.name %></h3>

    <% if subscription_plan.payable? %>
        <p><%= as_currency(subscription_plan.total_billing, subscription_plan.currency) %></p>
    <% else %>
        <p><%= t("subscriptions.listing.free") %></p>
    <% end %>
    <%= radio_button_tag "user_#{role}[subscription_plan_id]", subscription_plan.id, false, :id => "user_#{role}_subscription_plan_id" %>
    </div>
<% end %>

<%= custom_error_for_field(form, "subscription_plan_id") %>

<%#= link_to_main_page_article('subscriptions_for_supplier', {:popup => true}, {:id => 'subscriptions_modal_box'}) %>